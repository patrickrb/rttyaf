<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/material_dynamic_primary0"/>

    <stroke
        android:width="1dp"
        android:color="@color/power_progress_value" />
    <gradient
        android:angle="90"
        android:startColor="@color/power_progress_value"
        android:endColor="@color/power_progress_value"
        android:type="linear" />

    <!-- Rounded corners -->
    <corners android:radius="10dp" />
</shape>
